{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions --project playport-blr-2026"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.6.0",
    "razorpay": "^2.9.6"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "typescript": "~5.7.3"
  },
  "private": true
}
